{
  "name": "auxtho.github.io",
  "version": "1.0.0",
  "description": "Project configuration for the Auxtho website.",
  "scripts": {
    "build": "npx tailwindcss -i ./src/input.css -o ./assets/style.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.13"
  }
}
